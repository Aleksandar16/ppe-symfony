{% extends 'base.html.twig' %}

{% block content %}

    <div>
        <h2>Gestion des locatires</h2>
        <a type="button" class="btn btn-success" href="{{ path('ajout')}}">Nouveau locataire</a>
    </div>

    <table>
        <thead>
            <tr>
                <th>#</th>
                <th>Nom</th>
                <th>Prénom</th>
                <th>Nombre de locations</th>
                <th>Action</th>
            </tr>
        </thead>
        {% for mandataire in mandataire %}
            <tbody>
                <tr>
                    <td>{{ locataire.id }}</td>
                    <td>{{ locataire.name }}</td>
                    <td>{{ locataire.firstname }}</td>
                    <td>1</td>
                    <td><a type="button" class="btn btn-secondary" href="{{ path('show_locataire', {id: locataire.id})}}">Mettre à jour les données</a></td>
                </tr>
            </tbody>
        {% endfor %}
    </table>

{% endblock %}